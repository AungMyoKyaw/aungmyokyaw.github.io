import type { ReactNode } from 'react';

// The old Key Achievements section is now removed to avoid duplication. The improved version is in ProfessionalHighlights.
export default function HomepageFeatures(): ReactNode {
  return null;
}
